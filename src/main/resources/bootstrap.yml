spring:
  application:
    name: account-service
  
  cloud:
    config:
      uri: http://localhost:8080
management:  endpoints:    web:      exposure:        include: "*"
server:
  port: 9006 